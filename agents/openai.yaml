interface:
  display_name: "Seedance Shot Director"
  short_description: "Seedance 中文国风分镜、轻度表演增强与版本管理"
  default_prompt: "使用 $seedance-shot-director 处理剧本，沿用本任务配置，默认轻度表演增强并在确认后分镜，保持完整15秒五镜并优先完整收尾。"
policy:
  allow_implicit_invocation: true
