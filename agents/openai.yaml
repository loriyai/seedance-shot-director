interface:
  display_name: "Seedance Shot Director"
  short_description: "工业级 Seedance 2.0/2.5 漫剧分镜"
  default_prompt: "Use $seedance-shot-director to prepare Seedance 2.0/2.5 storyboards; after configuration, preserve each submitted segment as original V0 and ask whether to create a marked, reviewable script enhancement or proceed directly from the original, never generating shots from an enhancement until the user confirms it."
policy:
  allow_implicit_invocation: true
