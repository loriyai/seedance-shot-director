interface:
  display_name: "Seedance Shot Director"
  short_description: "工业级 Seedance 2.0/2.5 漫剧分镜"
  default_prompt: "Use $seedance-shot-director to split the supplied script or segment into N independent, paste-ready Seedance 2.0/2.5 generation blocks with shot tasks, physicalized actions, dialogue-capacity checks, spatial continuity, and conditional combat previs. Ask for 15s or 30s when no duration is specified, then keep that duration as the conversation default."
policy:
  allow_implicit_invocation: true
